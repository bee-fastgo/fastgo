<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bee.team.fastgo.mapper.ProjectDeployLogDoMapperExt">

    <!--查询项目部署列表-->
    <select id="findProjectDeployList" parameterType="java.lang.String" resultType="com.bee.team.fastgo.vo.project.ProjectDeployResVo">
        select tpdl.branchName,tpdl.deployLogId,tpdl.user,p.projectName,tpdl.gmt_create as gmtCreate from t_project_deploy_log tpdl
        inner join t_project p on p.projectCode = tpdl.projectCode
        where p.projectCode = #{projectCode}
    </select>

</mapper>
